{
  "mcpServers": {
    "wger": {
      "command": "wger-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "WGER_URL": "${WGER_URL:-http://localhost:8080}",
        "WGER_TOKEN": "${WGER_TOKEN}"
      }
    }
  }
}
